version: 2.1
jobs:
  print-hello:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: echo HELLO
  print-world:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run:  echo $_author

workflows:
  default:
    jobs:
      - print-hello
      - print-world:
          requires:
            - print-hello
